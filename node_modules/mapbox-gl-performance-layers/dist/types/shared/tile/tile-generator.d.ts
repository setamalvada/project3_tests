/// <reference types="mapbox-gl" />
import { BasicRenderer } from '../basic-renderer';
export declare class TileGenerator {
    private renderer;
    private frameBuffer;
    private matrix;
    constructor(renderer: BasicRenderer);
    initialise(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    dispose(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    generateTile(gl: WebGLRenderingContext, texture: WebGLTexture, textureWidth: number, textureHeight: number, x: number, y: number, zoom: number): void;
    private setMatrixValue;
}
