/// <reference types="mapbox-gl" />
import { BasicRenderer } from '../basic-renderer';
import { Bounds } from '../geometry-functions';
import * as glMatrix from 'gl-matrix';
export interface TileRendererOptions {
    numberOfTiles?: number;
    tileWidth?: number;
    tileHeight?: number;
}
export declare class TileRenderer {
    private renderer;
    private manager;
    private textureDrawer;
    private map;
    private readonly tileWidth;
    private readonly tileHeight;
    private readonly viewBounds;
    constructor(renderer: BasicRenderer, options: TileRendererOptions);
    markAllTilesOutdated(): void;
    markOutdatedTiles(bounds: Bounds): void;
    initialise(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    dispose(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    render(gl: WebGLRenderingContext, matrix: glMatrix.mat4 | number[], dataBounds: Bounds | null): void;
    private drawTile;
}
