/// <reference types="mapbox-gl" />
import { TileGenerator } from './tile-generator';
import { Bounds } from '../geometry-functions';
export declare class TileManager {
    private generator;
    private numberOfTiles;
    private tileWidth;
    private tileHeight;
    private tiles;
    constructor(generator: TileGenerator, numberOfTiles: number, tileWidth: number, tileHeight: number);
    markAllTilesOutdated(): void;
    markOutdatedTiles(bounds: Bounds): void;
    initialise(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    dispose(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    getTileTexture(gl: WebGLRenderingContext, x: number, y: number, zoom: number, dataBounds: Bounds | null): WebGLTexture | null;
    incrementAge(): void;
}
