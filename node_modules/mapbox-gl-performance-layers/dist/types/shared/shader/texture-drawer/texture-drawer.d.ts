import * as glMatrix from 'gl-matrix';
export declare class TextureDrawer {
    private program;
    private vertexBuffer;
    initialise(gl: WebGLRenderingContext): void;
    dispose(gl: WebGLRenderingContext): void;
    draw(gl: WebGLRenderingContext, texture: WebGLTexture, matrix: glMatrix.mat4 | number[], x: number, y: number, w: number, h: number): void;
}
