import { Feature } from 'geojson';
import { LineString } from 'geojson';
import { MultiLineString } from 'geojson';
import { EventData } from 'mapbox-gl';
import { MapMouseEvent } from 'mapbox-gl';
import { ResultsClickHandler } from './results-click-handler';
export declare type LineClickHandler<G extends LineString | MultiLineString, P> = (feature: Feature<G, P>, e: MapMouseEvent & EventData, closestPointOnLine: {
    x: number;
    y: number;
}) => void;
export declare function lineToResultsClickHandler<G extends LineString | MultiLineString, P>(onClick: LineClickHandler<G, P>): ResultsClickHandler<G, P>;
