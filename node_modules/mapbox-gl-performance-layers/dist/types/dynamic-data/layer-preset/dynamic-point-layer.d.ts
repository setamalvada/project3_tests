import { Point } from 'geojson';
import { MultiPoint } from 'geojson';
import { DynamicPointRendererOptions } from '../renderer-preset/dynamic-point-renderer';
import { PointStyle } from '../../shared/styles';
import { DynamicDataLayer } from '../dynamic-data-layer';
import { PointClickHandler } from '../../shared/click-handler/point-click-handler';
export interface DynamicPointLayerOptions<G extends Point | MultiPoint, P> extends DynamicPointRendererOptions<P> {
    id: string;
    onClick?: PointClickHandler<G, P>;
    clickSize?: number;
}
export declare function dynamicPointLayer<G extends Point | MultiPoint, P>(options: DynamicPointLayerOptions<G, P>): DynamicDataLayer<G, P, PointStyle>;
