import { FeatureCollection } from 'geojson';
import { Geometry } from 'geojson';
import { CustomLayerInterface } from 'mapbox-gl';
import { Renderer } from './renderer/renderer';
import { ClickProvider } from './click-provider/click-provider';
import { StyleOption } from '../shared/styles';
import { Visibility } from '../shared/visibility';
export interface StaticDataLayerOptions<G extends Geometry, P, S extends {}> {
    id: string;
    renderer: Renderer<G, P, S>;
    clickProvider?: ClickProvider<G, P>;
    renderingMode?: '2d' | '3d';
}
export declare class StaticDataLayer<G extends Geometry, P, S extends {}> implements CustomLayerInterface {
    private options;
    private map;
    private data;
    private styleOption;
    private visibility;
    constructor(options: StaticDataLayerOptions<G, P, S>);
    get id(): string;
    get renderingMode(): '2d' | '3d';
    get type(): 'custom';
    setDataAndStyle(data: FeatureCollection<G, P>, styleOption: StyleOption<G, P, S>): void;
    setData(data: FeatureCollection<G, P>): void;
    setStyle(styleOption: StyleOption<G, P, S>): void;
    clearData(): void;
    setVisibility(visibility: Visibility): void;
    isVisible(): boolean;
    onAdd(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    onRemove(map: mapboxgl.Map, gl: WebGLRenderingContext): void;
    prerender(gl: WebGLRenderingContext, matrix: number[]): void;
    render(gl: WebGLRenderingContext, matrix: number[]): void;
}
