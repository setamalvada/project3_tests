/// <reference types="mapbox-gl" />
import { FeatureCollection } from 'geojson';
import { MultiPoint } from 'geojson';
import { Point } from 'geojson';
import { ClickProvider } from './click-provider';
import { PointClickHandler } from '../../shared/click-handler/point-click-handler';
import { Visibility } from '../../shared/visibility';
export declare class KdBushClickProvider<G extends Point | MultiPoint, P> implements ClickProvider<G, P> {
    private onClick;
    private clickSize?;
    private map;
    private data;
    private index;
    private visibility;
    constructor(onClick: PointClickHandler<G, P>, clickSize?: number | undefined);
    setData(data: FeatureCollection<G, P>): void;
    clearData(): void;
    initialise(map: mapboxgl.Map): void;
    dispose(map: mapboxgl.Map): void;
    setVisibility(visibility: Visibility): void;
    private clickHandler;
}
