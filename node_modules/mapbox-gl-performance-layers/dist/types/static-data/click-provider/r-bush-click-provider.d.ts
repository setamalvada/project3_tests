/// <reference types="mapbox-gl" />
import { FeatureCollection } from 'geojson';
import { Geometry } from 'geojson';
import { ClickProvider } from './click-provider';
import { Visibility } from '../../shared/visibility';
import { ResultsClickHandler } from '../../shared/click-handler/results-click-handler';
export declare class RBushClickProvider<G extends Geometry, P> implements ClickProvider<G, P> {
    private resultsHandler;
    private clickSize?;
    private map;
    private tree;
    private visibility;
    constructor(resultsHandler: ResultsClickHandler<G, P>, clickSize?: number | undefined);
    setData(data: FeatureCollection<G, P>): void;
    clearData(): void;
    initialise(map: mapboxgl.Map): void;
    dispose(map: mapboxgl.Map): void;
    setVisibility(visibility: Visibility): void;
    private clickHandler;
}
