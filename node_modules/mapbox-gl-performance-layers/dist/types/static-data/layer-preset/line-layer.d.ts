import { LineString } from 'geojson';
import { MultiLineString } from 'geojson';
import { LineRendererOptions } from '../renderer-preset/line-renderer';
import { StaticDataLayer } from '../static-data-layer';
import { LineStyle } from '../../shared/styles';
import { LineClickHandler } from '../../shared/click-handler/line-click-handler';
export interface LineLayerOptions<G extends LineString | MultiLineString, P> extends LineRendererOptions<P> {
    id: string;
    onClick?: LineClickHandler<G, P>;
    clickSize?: number;
}
export declare function lineLayer<G extends LineString | MultiLineString, P>(options: LineLayerOptions<G, P>): StaticDataLayer<G, P, LineStyle>;
